<?php

namespace JsonSeo\Exception;

/**
 * Ответа не дождались за отведённое время.
 *
 * Такой запрос не повторяется автоматически: обрыв на стороне клиента не
 * отменяет работу на стороне сервиса — выдача всё равно будет собрана и
 * оплачена, и второй заход стоил бы ещё раз. Если ответ нужен, поднимите
 * timeout, а не число повторов.
 */
class TimeoutException extends TransportException {}
